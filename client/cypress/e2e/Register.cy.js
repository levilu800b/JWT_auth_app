describe('The username Page', () => {
	it('successfully loads', () => {
		cy.visit('/');
	});
	
	it('should click on the Register now if the username does not exist', () => {
		cy.visit('/')
		
	})
});
